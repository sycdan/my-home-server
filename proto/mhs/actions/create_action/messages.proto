edition = "2024";
package mhs.actions.create_action;

message CreateActionRequest {
  string action_name = 1; // e.g.: "Do Stuff"
  string domain_path = 2; // e.g.: "mhs/example" (does not have to exist)
}

message CreateActionResponse {
  bool success = 1;
  repeated string errors = 2;
}