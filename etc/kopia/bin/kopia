#!/usr/bin/env bash
# This is the main kopia CLI entrypoint, for commands like `kopia snapshot create`, etc.
main() {
  cd "$SERVICE_DIR"
  docker compose exec $SERVICE_KEY kopia "$@"
}

source "$(cd "$(dirname "${BASH_SOURCE[0]}")/../../.." && pwd)/lib/cli.sh"