#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
ROOT_DIR="$(cd "$SCRIPT_DIR/../" && pwd)"
SERVICES_DIR="$ROOT_DIR/services"
$ROOT_DIR/run $SERVICES_DIR/kopia/ctl/shell