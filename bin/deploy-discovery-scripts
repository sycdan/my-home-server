#!/usr/bin/env python

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

from mhs.lan.discover_devices.cli import main

if __name__ == "__main__":
  main()
else:
  print("This script is intended to be executed directly.")
  sys.exit(1)
