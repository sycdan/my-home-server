# Immich Configuration Example
# Copy this file to .env and update with your values
# Full documentation: https://docs.immich.app/install/environment-variables

# Immich version to use (e.g., release, v2.1.0, latest)
IMMICH_VERSION=release

# Timezone for the service
# See: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones#List
TZ=America/New_York

# Database Configuration
# IMPORTANT: Change DB_PASSWORD to a strong random password!
# Use only characters A-Za-z0-9 (no special chars or spaces)
DB_PASSWORD=postgres_change_me_to_strong_password
DB_USERNAME=postgres
DB_DATABASE_NAME=immich

# Storage Locations
# Paths where Immich will store uploaded photos and database data
# Can be absolute paths or relative to where docker-compose is run from
# Network shares are NOT supported for database
UPLOAD_LOCATION=./immich-data
DB_DATA_LOCATION=./immich-db

# Optional: REDIS configuration (defaults shown below)
REDIS_HOSTNAME=redis
REDIS_PORT=6379
REDIS_DBINDEX=0
